# Archivos sueltos de tareas: solo contienen el link a GitHub y se entregan por otra vía.
/*.txt

# Material de FretPath: ya no se necesita para el curso.
fretPath-estudiantes/

# Consignas del curso y material de apoyo local: se entregan por otra vía y no se suben.
*.docx
semana2/docs-referencia/

# Archivos de bloqueo que deja LibreOffice al abrir un documento.
.~lock.*#

# Trabajo local de la version con MongoDB: se queda en el disco, no se sube.
semana6-mongoDB/
